const API_URL = 'http://127.0.0.1:8000'; const form=document.querySelector('#signup-form'); const message=document.querySelector('#message');
form.addEventListener('submit',async(event)=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;message.textContent='';const body={name:document.querySelector('#name').value.trim(),email:document.querySelector('#email').value.trim(),password:document.querySelector('#password').value};try{const response=await fetch(`${API_URL}/auth/register`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await response.json();if(!response.ok)throw new Error(data.detail||'Unable to create account.');message.textContent='Account created. Redirecting to sign in…';message.className='message success';setTimeout(()=>window.location.href='login.html',900);}catch(error){message.textContent=error.message.includes('Failed to fetch')?'Could not reach the API. Is FastAPI running?':error.message;message.className='message error';button.disabled=false;}});
